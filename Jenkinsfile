pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/AnnSobczak/app-1-ops.git', branch: 'main'
            }
        }
    }
}
